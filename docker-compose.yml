
services:
  db:
    image: mariadb:11
    container_name: symfony-db
    restart: always
    environment:
      MYSQL_ROOT_PASSWORD: root
      MYSQL_DATABASE: kanban_db
      MYSQL_USER: symfony
      MYSQL_PASSWORD: symfony
    ports:
      - "3306:3306"
    volumes:
      - db_data:/var/lib/mysql
    networks:
      - app_network

  backend:
    build:
      context: ./backend
      dockerfile: Dockerfile
    container_name: symfony-backend
    volumes:
      - ./backend/public:/var/www/html/public        # assets & index.php
      - ./backend/src:/var/www/html/src              # ton code Symfony
      - ./backend/config:/var/www/html/config        # routes + services
      - ./backend/templates:/var/www/html/templates  # fichiers Twig éventuels
      - ./backend/bin:/var/www/html/bin              # console Symfony
    ports:
      - "8080:80"
    depends_on:
      - db
    networks:
      - app_network
    environment:
      - APP_ENV=prod

  # frontend:
  #   build:
  #     context: ./frontend
  #     dockerfile: Dockerfile
  #   container_name: vue-frontend
  #   ports:
  #     - "5173:80"
  #   depends_on:
  #     - backend
  #   networks:
  #     - app_network

volumes:
  db_data:

networks:
  app_network:
    driver: bridge
